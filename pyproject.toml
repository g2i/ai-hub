[project]
name = "api-proxy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.12",
    "hypercorn>=0.17.3",
    "python-dotenv>=1.1.0",
    "python-multipart>=0.0.20",
]
